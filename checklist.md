# 全场景AI日志分析平台 - RAG功能验证 Checklist

## 1. 数据库与环境验证
- [ ] MySQL 数据库中 `ai_knowledge_base`, `ai_kb_document`, `ai_kb_segment`, `ai_agent_kb_relation` 表创建成功且字段正确。
- [ ] 向量数据库（Milvus/pgvector）连接成功，对应的 Collection/Table 已创建且具备向量索引能力。
- [ ] Spring Boot 后端启动无报错，LangChain4j 依赖及向量库 SDK 加载正常。

## 2. 后端逻辑验证
- [ ] 若依基础功能：知识库、文档的增删改查接口通过 Postman/Swagger 测试。
- [ ] 解析流水线：上传 PDF/TXT 格式的运维文档，后端能正确读取并按设定的 `chunk_size` 完成切分。
- [ ] 向量化与存储：切分后的文本块能成功调用 Embedding API 转换为向量，并成功写入向量数据库。
- [ ] 异步状态管理：大文件解析过程中，文档状态能正确经历 `解析中` -> `成功`/`失败` 的流转。
- [ ] 检索接口测试：调用 `/ai/kb/hit-test` 接口，传入测试日志，能成功召回向量库中最相似的 Top-K 文本块并返回正确得分。
- [ ] RAG 提示词构建：对话接口在有知识库关联时，能正确组装包含 `{context}` 的 Prompt。

## 3. 前端交互验证 (Vue3)
- [ ] 菜单显示正常，列表页查询、分页功能正常。
- [ ] 文档上传组件能正常工作，分段参数（大小、重叠量）能成功传给后端。
- [ ] 文档解析状态能在前端列表实时/刷新后正确展示。
- [ ] 命中测试界面：滑块和输入框功能正常，召回结果能以卡片形式清晰展示相似度及匹配段落。
- [ ] 智能体表单：能成功加载知识库下拉列表，并能正确保存关联数据至数据库。
- [ ] 聊天界面：AI 回答后，能正确解析返回的 Citations 数据，折叠面板可点开查看引用来源。

## 4. 全链路业务验证
- [ ] **场景测试 1**：将一份包含特定错误码解决思路的文档存入知识库并绑定给“日志分析助手”。向助手发送该错误码，助手能基于文档内容给出解决方案，而非产生幻觉。
- [ ] **场景测试 2**：在命中测试中输入随机乱码日志，若相似度低于阈值，系统应不召回任何知识库段落。
- [ ] **场景测试 3**：管理员在前端修改了某一个分段的文本内容，重新进行命中测试，新修改的内容能够生效被检索到。